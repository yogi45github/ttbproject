{% block wizardsearch %}

    <div class="find">
        <h2>{{ 'search by wizard'|t }}</h2>
        <form name="findByParameterIdentifocation" id="findByParameterIdentifocation">
            <table border="0" class="wizard-table">
                <tbody>
                    {% for field in wizardFields %}
                        <tr class="yqservice_SelectedRow">
                            <td>{{ field.name }}</td>
                            <td>
                               <select class="catalog_search_input select" {{ field.determined ? 'disabled' : '' }} name="Select" onchange="openWizard(this.options[this.selectedIndex].value, '{{ cataloginfo.code }}'); return false;" style="width: 200px;">
                                   {% if field.determined %}
                                   {% else %}
                                       <option value="null"></option>
                                   {% endif %}
                                   {% for value in field.options %}
                                       <option value="{{ value.key }}">{{value.value }}</option>
                                   {% endfor %}
                                </select>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </form>
    </div>


    <script type="text/javascript">
        jQuery(window).bind('pageshow', function() {
            jQuery('form#findByParameterIdentifocation').find('select').val('');
        })

    </script>

{% endblock %}